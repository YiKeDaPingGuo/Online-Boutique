apiVersion: v1
kind: Service
metadata:
  name: oversee
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8080"
    prometheus.io/path: "/actuator/prometheus"
spec:
  selector:
    app: oversee
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP